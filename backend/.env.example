# NutriGo 后端环境变量示例
#
# 后端从进程环境读取以下变量（不自动加载 .env 文件）：
#   - 本地开发：直接 export，或用 docker compose / 部署脚本注入
#   - 生产部署：见 deploy/compose/.env.production.example（docker-compose 注入）
#
# 开发环境（APP_ENV != production）可留空使用内置默认值；
# 生产环境必须设置强随机的 JWT_SECRET 与 INTERNAL_TOKEN，否则启动失败。

# 运行环境：development / production
APP_ENV=development

# JWT 签名密钥（须与 agent/.env 的 JWT_SECRET 一致）
# 生产：openssl rand -hex 32 生成强随机值
JWT_SECRET=

# 内部服务鉴权 token（Python Agent 调用后端时携带 X-Internal-Token）
# 生产：openssl rand -hex 32 生成强随机值
INTERNAL_TOKEN=

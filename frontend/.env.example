# 开发时留空：App 和浏览器预览通过 Vite 代理连接电脑上的 Go / Agent。
# 打包 App 前设置已部署的 HTTPS API 根地址，不带 /api 或 /agent-api。
# 例：VITE_API_BASE_URL=https://api.your-domain.com
# 也可使用有效公网 IP HTTPS 证书的地址；构建后更换地址需重新打包。
# VITE_* 会随安装包公开，不得填写 JWT 签名密钥、内部令牌或 AI API Key。
VITE_API_BASE_URL=
